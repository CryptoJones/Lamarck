# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/

# Lamarck-specific
# Model checkpoints — never commit. Multi-hundred-GB binaries.
checkpoints/
weights/
*.safetensors
*.bin
*.pt

# Generated curricula. Reproducible from seed + G0; not source.
curricula/*.jsonl
!curricula/.gitkeep

# arXiv corpus pull. Multi-GB; reproducible from the fetch script
# in scripts/data_collection/. See seed-data/arxiv-ml/README.md.
seed-data/arxiv-ml/full/
seed-data/arxiv-ml/sample/

# Eval suite cache + run outputs.
.eval-cache/
runs/

# Local config that might carry secrets.
.env
.env.*
*.pem
*.key
