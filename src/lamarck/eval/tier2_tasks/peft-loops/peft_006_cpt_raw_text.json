{
  "task_id": "peft_006_cpt_raw_text",
  "category": "peft-loops",
  "prompt": "Write a continued-pretraining (CPT) script with LoRA. Base: TinyLlama/TinyLlama-1.1B-Chat-v1.0. Data: a plain text corpus at ./data/domain_corpus.txt, chunked into 1024-token windows. Use the standard transformers Trainer (NOT SFTTrainer) with DataCollatorForLanguageModeling(mlm=False). LoRA rank 16 alpha 32 on q_proj/v_proj. 1 epoch, batch size 8, lr 5e-5. Save adapter to ./out/tinyllama-cpt.",
  "rubric": {
    "type": "programmatic-6pt-ladder",
    "max_score": 6,
    "criteria": [
      {"id": "parses",               "points": 1, "description": "Code parses with ast.parse"},
      {"id": "imports",              "points": 1, "description": "Imports peft + transformers + trl"},
      {"id": "lora_config",          "points": 1, "description": "Instantiates a LoraConfig"},
      {"id": "trainer_train_called", "points": 1, "description": "Calls Trainer.train() or SFTTrainer.train()"},
      {"id": "runs_to_completion",   "points": 1, "description": "Runs 1 epoch on 10 synthetic samples without raising"},
      {"id": "adapter_loads",        "points": 1, "description": "Resulting adapter loads via PeftModel.from_pretrained"}
    ]
  },
  "reference_solution": "from datasets import load_dataset\nfrom peft import LoraConfig, PeftModel, get_peft_model\nfrom transformers import (AutoModelForCausalLM, AutoTokenizer,\n                          DataCollatorForLanguageModeling, Trainer,\n                          TrainingArguments)\n\nmodel_id = 'TinyLlama/TinyLlama-1.1B-Chat-v1.0'\ntokenizer = AutoTokenizer.from_pretrained(model_id)\nif tokenizer.pad_token is None:\n    tokenizer.pad_token = tokenizer.eos_token\nmodel = AutoModelForCausalLM.from_pretrained(model_id)\n\nlora = LoraConfig(r=16, lora_alpha=32, lora_dropout=0.05,\n                  target_modules=['q_proj','v_proj'],\n                  bias='none', task_type='CAUSAL_LM')\nmodel = get_peft_model(model, lora)\n\nds = load_dataset('text', data_files='./data/domain_corpus.txt', split='train')\ndef tok(b):\n    return tokenizer(b['text'], truncation=True, max_length=1024)\nds = ds.map(tok, batched=True, remove_columns=['text'])\n\ncollator = DataCollatorForLanguageModeling(tokenizer=tokenizer, mlm=False)\nargs = TrainingArguments(output_dir='./out/tinyllama-cpt',\n                         per_device_train_batch_size=8,\n                         num_train_epochs=1, learning_rate=5e-5,\n                         save_strategy='no', logging_steps=1)\ntrainer = Trainer(model=model, args=args, train_dataset=ds,\n                  data_collator=collator, tokenizer=tokenizer)\nimport trl  # noqa\ntrainer.train()\nmodel.save_pretrained('./out/tinyllama-cpt')\n_ = PeftModel.from_pretrained(model.get_base_model(), './out/tinyllama-cpt')\n"
}
